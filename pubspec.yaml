name: emulator_palapa
description: >-
  Console emulation for Flutter. Wraps libretro cores behind a Dart API, with
  save states, period display shaders and a seam for a language model.
version: 0.1.0

repository: https://github.com/palapa-ai/emulators
issue_tracker: https://github.com/palapa-ai/emulators/issues

topics:
  - emulator
  - libretro
  - games
  - ffi

environment:
  sdk: ^3.12.0
  flutter: ">=3.3.0"

dependencies:
  common_mvvm:
    git:
      url: https://github.com/palapa-ai/common.git
      path: packages/mvvm
      ref: 1289e4331321cbe2e91bd5955fa5a963b38f13e8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  shaders:
    - shaders/vhs.frag
    - shaders/crt.frag

  plugin:
    platforms:
      macos:
        ffiPlugin: true
      ios:
        ffiPlugin: true
